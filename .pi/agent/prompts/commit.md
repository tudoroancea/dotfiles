---
description: Commit the current changes
---
Commit the changes made in this session with an appropriate commit message.
Ignore other changes and do not change their staged/unstaged status. 
Make the message short and don't bother with prefixes such as `fix(ci)`. If the commits is big enough to warrant explanations, put them in the commit description not title.

$@
