[projects."/Users/tudoroancea/dev/alloy"]
approved-commands = [
    "uv run ty check",
    "uv run ruff format --check",
    "uv run ruff check",
    "uv run pytest -n=auto",
    "uv run pytest -n=auto tests",
]

[projects."github.com/PREDICT-EPFL/alloy"]
approved-commands = [
    "wt step copy-ignored",
    "uv run ty check",
    "uv run ruff format --check",
    "uv run ruff check",
    "uv run pytest -n=auto tests",
]

[projects."github.com/PREDICT-EPFL/anvil"]
approved-commands = [
    "uv run ruff format --check",
    "uv run ruff check",
    "uv run pytest -n=auto --ignore=tests/integration",
    "uv run pytest tests/integration",
    "uv run ty check",
    "wt step copy-ignored",
]

[projects."github.com/tudoroancea/babyfoot_league"]
approved-commands = [
    "bun run typecheck",
    "bun run lint",
    "bun run fmt",
    "bun run test:e2e",
]

[projects."github.com/tudoroancea/pi-setup"]
approved-commands = [
    "wt step copy-ignored --require-include",
    """
set -eu
test -e agent/npm || ln -s {{ primary_worktree_path }}/agent/npm agent/npm
test -e agent/git || ln -s {{ primary_worktree_path }}/agent/git agent/git
""",
]
